module Selection.Construction where

-- TODO: move code for generating a set of constraints here
-- (code for defining/checking constraints stays in Constraints, code for
-- building the constraint set goes here)
